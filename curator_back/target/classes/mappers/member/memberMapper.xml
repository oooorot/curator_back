<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.root.mybatis.member.MemberMapper">
	<resultMap type="com.web.root.member.dto.MemberDTO" id="member">
		<result property="memberSeq" column="member_seq"/>
		<result property="memberEmail" column="member_email"/>
		<result property="memberPw" column="member_pw"/>
		<result property="memberNickname" column="member_nickname"/>
		<result property="memberName" column="member_name"/>
		<result property="memberPhone" column="member_phone"/>
		<result property="memberAddr" column="member_addr"/>
		<result property="memberGrade" column="member_grade"/>
		<result property="memberDate" column="member_date"/>
	</resultMap>
	
	<select id="userCheck" resultMap="member">
		SELECT member_seq, member_email, member_pw, member_grade FROM member_info WHERE member_email=#{memberEmail} AND member_pw=#{memberPw}
	</select>
	
	<select id="memberInfo" resultMap="member">
		SELECT * FROM member_info
	</select>
	
	<!-- 
		private int memberSeq;
	private String memberEmail;
	private String memberPw;
	private String memberNickname;
	private String memberName;
	private String memberPhone;
	privat e String memberAddr;
	private int memberGrade;
	private String memberDate; -->
	<insert id="register">
		INSERT INTO member_info VALUES(member_info_seq.nextval,#{memberEmail},#{memberPw},#{memberNickname},#{memberName},#{memberPhone},#{memberAddr},#{memberGrade},current_date)
	</insert>

</mapper>
